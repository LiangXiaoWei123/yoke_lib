# 两灯颜色调节示例

示例创建 I2C0 总线并校验模块设备 ID，然后演示编码器、按键和两颗板载 WS2812：

- 启动时选择 LED1。
- 短按编码器按键：在 LED1 和 LED2 之间切换。
- 旋转编码器：沿色相环调整当前选中 LED 的颜色。
- 按住按键并旋转：同时调整两颗 LED 的亮度（0～100%，每格 5%）。

默认连线：SDA=GPIO15、SCL=GPIO16。修改 `main/example_main.c` 中的 GPIO 定义以匹配实际硬件。
